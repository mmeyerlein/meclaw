# templates/

Reference topologies you can instantiate. Every one of them is pure DSL -- directories,
`config.json` files and edges. No new Rust, no plugin API, no runtime dependency on this
folder at all.

## What a template is

A template is a physical subtree on disk: a `template.json` at its root (name, version and a
four-slot description that the colony serves over `/colony/templates`), one `config.json` per
cell, and sub-cells as nested directories. That is the whole format. What you read here is
exactly what the substrate reads.

**Instantiation COPIES.** When a mutation names a template, the colony materializes the subtree
into your colony root -- substituting `${uuid7:*}` on disk, keeping `${VAR}` as a late-binding
token -- and from that moment your instance is yours. It has no link back to this library. This
folder is not on the runtime path of a booted colony; delete it and every colony that was built
from it keeps running.

A template is also self-contained: it has no edges leaving its own subtree. Wiring it into a
colony is the job of the mutation that instantiates it (see below).

## The hive boundary

Every template that is a hive is bound by one rule, and it is a requirement rather than a
convention:

> An edge is laid at the HIVE. Access from outside happens abstractly and functionally, never
> structurally and directly. An edge asks for something by content, without knowing the
> structure. The inner edge that receives that request is what knows what to do about it.

Ruled 2026-08-18 ([#197](https://github.com/mmeyerlein/meclaw/issues/197),
[#200](https://github.com/mmeyerlein/meclaw/issues/200)) and specified in
[`../docs/meclaw-overview.md`](../docs/meclaw-overview.md) § The hive boundary. **It binds
every hive and every template** -- a hive that declares no `params.ports` is one the substrate
does not enforce it on, not one it does not apply to. Which templates have arrived and which
have not is the table in that section.

### Authoring a hive template: five things, all checkable

1. **`params.ports: []`** in the hive marker's `config.json`. The empty list is not an omission,
   it is the statement "the hive path is the only address". No `ports` key at all means unsealed,
   which means unfinished.
2. **A door per accepted lane** -- an edge with `"from": "."`, a `condition` testing the lane,
   and a `to` naming the inner cell that serves it. This is the only place the structure of the
   inside may be known.
3. **`params.contract`** with `accepts` and `emits`, and **lane names that say what the caller
   wants, never where it lands inside**. `writer`, `recall`, `render`, `policy` are inner cell
   names; renaming a port into a lane of the same name satisfies the letter of rule 1 and misses
   its point. The test: does the name survive a reimplementation of the inside?
4. **No address in your prose that the boundary would refuse.** `template.json` and the README
   describe lanes, not cells -- the `description` slots are the interface a caller reads, and a
   `from:`/`to:` in them is a wiring instruction, which is why
   [#203](https://github.com/mmeyerlein/meclaw/issues/203) was a defect and not a typo. The gate
   is `crates/meclaw-cells/tests/gh203_documented_port_addresses.rs`, and it asks the real
   boundary validator.
5. **No context key you set on one of your own edges survives an exit edge.** A hive has no
   `cell.db`, so a cell that has to remember which round trip an answer belongs to promotes a
   marker to `context` on an interior edge -- `<name>_origin`, plus a phase and a carry. Context
   is persistent for the life of a chain and nothing removes those keys again, so every edge with
   `"to": "."` clears the hive's own keys with `delete_context`, or the marker rides out on the
   answer and the next request re-enters wearing it: the cell that reads it before it reads the
   inbound lane then dispatches a stranger as its own echo
   ([#481](https://github.com/mmeyerlein/meclaw/issues/481),
   [#490](https://github.com/mmeyerlein/meclaw/issues/490),
   [#494](https://github.com/mmeyerlein/meclaw/issues/494)). Two exemptions, both named rather
   than inferred: the context vocabulary the templates pass **between** hives on purpose
   (`session_id`, `channel`, `turn_id`, ...), and a key a round trip **out** of the hive has to
   bring back because the answer re-enters through a door that does not re-establish it. The
   gate is `crates/meclaw-cells/tests/gh494_no_interior_marker_leaves_a_hive.rs`, which carries
   both lists; `workshop/tools/hive_context_sweep.py` is the same sweep on the command line.

### Wiring one: address the hive, name the lane

An edge from outside points at the hive path and carries its request on `hop.route`:

```json
{"from": "./ingress", "to": "./agent",
 "modifier": {"set_hop": {"route": "'in_turn'"}}}
```

Which lanes a hive accepts and emits is in its `params.contract`. If you find yourself needing
a segment after the hive name, the answer is not a longer address -- it is a lane whose name
says what you wanted, and a door inside the hive that knows where that belongs.

### Writing a cell a tool round will call

A tool cell answers on the collector's `in_tool` lane, and what it may hand back is its
**`messages[]`**: one `tool_result` turn per call it answers, each carrying the `id` of
that call. All of them travel -- a tool that got a bundle of calls in one message may
answer the whole bundle in one message.

Nothing else travels. A `system` slot or a top-level body slot on that lane is dropped,
and deliberately so: `system.*` reaching an `llm` cell is upserted into that cell's own
`cell.db` and stands in the prompt until something overwrites the same slot path, which
makes it durable state of the agent rather than evidence of one round. A tool with
structure to return serialises it into the text of its result; a producer that means to
install a lasting constraint addresses the `llm` cell's `system` tree on a lane of its own
instead. The long version, with the reasoning and the worked example, is
[`collector/README.md` § What a tool result may carry](collector/README.md).

## The library

**This table is the published library, and it is a subset of what a working tree may
carry.** Every template the export ships has a row here -- that is a gate, not a habit
(`crates/meclaw-cells/tests/gh235_every_public_template_has_a_row.rs` compares the rows
against the export's own allow-list, and its twin `gh235_readme_library_table.rs` compares
each row's version against that template's `template.json`). A development tree can hold
further directories under `templates/` that are deliberately not published: they carry no
compatibility promise, they are not referenced from here, and nothing in this table
depends on one.

| Template | Version | What it is |
|---|---|---|
| [`access`](access/) | 2.4.3 | The capability broker as one hive: an agent may ask in natural language, what travels on the wire is a **handle**, and a credential leaves the vault beside it only SEALED, under a key the requester minted for that one call. Six cells, no model -- every verdict is a comparison. Seven seeded policy rows, five of them disabled: what ships enabled is the pair a colony cannot start without (ruling R-Policy-Default, 2026-08-28), narrow and scoped to `/os/orgs`. Since 2.3.0 a request may also be CHECK-ONLY -- a verdict and no grant, because a grant nobody spends is a bearer row with an expiry date -- and a rule may name `scope_prefix`, a reserved key compared as a PATH prefix rather than for equality. Since 2.3.1 the seed carries a second row for the submitter to ask over -- `code.author`, whether a manifest may bring executable behaviour with it -- disabled like every other, so a fresh colony applies manifests and authors no code (#446). Since 2.4.1 a caller may ask TWICE on one message chain: the interior round-trip markers no longer leave the hive, and an inbound lane is recognised as a request whatever the context carries -- before it, every second question was answered out of the first one's carry, which made `code.author` unreachable in any colony (#481). Since 2.4.2 the seed also carries the SHELL's own scope -- `colony.mutate.shell`, `scope_prefix: "/os"`, priority 90 and switched OFF: every other row is scoped `/os/orgs`, so a declaration whose scope root is the colony itself (registering a template class, for one) could never pass a colony's own front door. `/os` is the superset, so it ships off and is turned on with a `seed_rows` manifest by whoever means it (#514). Since 2.4.3 the four rows the submitter asks over name `/os/operator/submit`: the submitter is an occupant of the front door now (#556), and a requester the shell's edge can no longer promote is a rule that matches nothing. |
| [`affinity`](affinity/) | 3.2.0 | The curated record of the people and agents a colony knows: whole AIeOS 1.1.0 documents in a store, plus the four things that standard has no vocabulary for -- relations, trust, disclosure and an append-only audit. One writer, one reader, no model, and the audience filter exists in exactly one place. Since 3.1.1 the seeded subscription ships `inactive`: a row is half a subscription, the other half is an edge only a mutation can draw (#458). Since 3.2.0 the record TRAVELS: `in_export` walks all nine tables out as a versioned document and `in_import` takes one part back into a running hive, so a member reborn from an export knows who its people are instead of only what it was told (#471). |
| [`archive-bridge`](archive-bridge/) | 1.0.0 | Turns an llm's last answer into a store-native insert -- and swallows the store's reply echo, so an append-only archive costs one cell instead of a loop. |
| [`argus`](argus/) | 1.0.0 | The colony's control loop and its WATCHER, which is the same thing seen from both ends: charter, deterministic measurement, a judge that simulates before it decides, a params update to the cell it names, an immediate health check, and keep-or-revert after the window. **A receipt for every tick** -- including the tick that had nothing to do and the cycle that failed at a store reject, because a loop that only writes down its successes is a loop nobody can audit. Ships with every goal disabled: a freshly grown `argus` measures nothing and changes nothing until an operator turns a charter row on. Renamed from `steward` in [#462](https://github.com/mmeyerlein/meclaw/issues/462). |
| [`assistant`](assistant/) | 2.4.0 | One generation of one person's agent, as a composition level: the conversation surface (`talky`, a ref to `talky`), the reasoning core (`cogny`) and the tool surface (`tools`). **Three refs and no container at all** -- eight lanes in, eight out, plus four corridor lanes (`in_pack`/`pack_ack` and `recall`/`in_bundle`, which name connect points and dock on the two occupants rather than on this rim), and twenty-nine edges. The two counts move independently and that is the level rule working: an occupant lane both halves of which stay inside costs edges here and nothing at the boundary (#464), while one that nothing inside consumes crosses and is declared (#475). Since 2.0.0 the CHANNELS are gone from this level (#454): a channel belongs to the person, so it stands in `<member>/channels` and reaches this generation by addressing its path. Two lanes moved with it and both fall out of the derivation rule: `turn` was removed (the raw wire is outside now and never crosses this level) and `answer` was added (the connector that used to consume it inside is outside too). Taking a lane and an address away is the first digit. The level is complete at birth: there is no per-channel follow-up mutation and no intermediate state in which a generation stands but is silent. No memory, no screen, no channel: those belong to the member (GH #122, #454). Since 2.1.0 an identity does REACH this level -- `in_pack` fans a durable `system.*` slot out to the surface and the core alike, answered once per occupant on `pack_ack` (#458) -- but it is still not kept here: the record it comes from is the member's. Since 2.4.0 the level no longer CARRIES that pack either: it declares a connect point for it (`"at": ["./talky", "./cogny"]`) and the sender draws one v-lane per rim straight from the member's own `./affinity`, one back for the receipt, so four pass-through edges fall away (#559, #561). Since 2.1.0 the level also passes the transfer lanes of its surface's session keeper straight through -- `in_export` and `in_import` in, `dump` out, three plain edges -- so the one store a generation holds that a rebuild cannot recompute can leave and come back (#475). Since 2.1.0 it also decides WHICH MODEL each of its two brains infers with, and it is the only place that can: `talky` and `cogny` both read `ctx.model` in their brain, both are right to standalone, and a level that references both meets ONE flat ctx -- so both resolved the same key and the conversation surface ran the reasoning model, every turn paying core latency for the half whose whole job is to answer fast. The level's ref marker names `ctx.model_surface` for the cell it references and the level declares that key STRICT, its first `requires` block of its own; `model` and `model_fast` stay inherited through the ref chain and neither occupant moved (#516). **`ctx` therefore carries three model keys, not two.** Since 2.2.0 it draws the SECOND `schemas` / `in_menu` pair -- surface to core and back -- because `consult_cogny` is not a tool of any hive but the errand this level routes, so only the side that answers it can declare it; the declared list is the level's, on the same ref marker that names the surface's model (#529). `ask_memory` is retired in the same act: the lookup class assumed the core could answer a memory question, and the surface answers one a hop away with its own `memory_recall` (#530). Since 2.4.0 both surfaces carry that connect point (#560) and this level declares nothing about the lane: a generation takes no influence on a credential, so the v-lane skips it, which is exactly what the transparent row of the rule table means (#559, development-rules § 8b). Since 2.4.0 the memory road does not cross this level either: `recall` and `in_bundle` keep their contract entries and gain `at: ["./talky", "./cogny"]`, the four pass-through edges are gone (thirty-three become twenty-nine), and one v-lane per asker per direction delivers in one hop instead of three (ADR-0020, #562). Vouching is not influence: the level that DOES something to a recall is the member, which declares the same lane without a connect point and is therefore a hop nothing may skip. |
| [`builder`](builder/) | 1.6.0 | The intake that turns a structural wish into a MANIFEST -- an ordered list of mutation declarations, ready to be submitted by whoever asked for it. Two classes on one lane: a fast lane of predefined parameterised recipes that runs without calling a model at all, and a design lane that consults `builder-librarian` and asks one. It never applies anything, and that is a property of the FILES rather than a promise in a README: no cell in it has an edge onto the control plane, and `/colony/mutations` is not an endpoint a mutation may draw on any scope (ADR-0015). Since 1.1.2 its brief also carries the tool principle in the HEAD, where a corpus outage cannot take it away: a tool is a cell plus a name edge in the tools hive, and a cell that uses one declares it (#464). Since 1.2.0 a LEVEL is a recipe: `grow_level` renders the transit edge set a new organisation, member, assistant, channel, screen or app gets from its parent -- the same set every time, pinned against `examples/organism` byte for byte -- while the template choice stays a catalogue question; since 1.2.1 the two container levels render the set they actually have, `in_export` and the three receipt lanes included, held to `examples/memory-import/build_import.py` element for element (#470); since 1.3.0 a level can be grown ASLEEP -- `birth` is a recipe parameter and a channel defaults to it, so a connector no longer has to be born awake and put back to sleep by a second act (#472) -- and an assistant can be grown with the identity door beside it, the push edge from the member's own affinity record plus its receipt drain, drawn only when the wish asks for it (#473). Its grammar learned `override_params` (addressed), `ref` as a declaration form, path depth beyond one segment and the address rule v1; and the composer may now ASK for a model id instead of inventing one, which comes back on `error` as `wish_incomplete` (#466). Since 1.3.1 the question and the instruction tree are PARKED in the round table by the brief and handed back by the fan-in on every round: the design lane briefs round 0 and the loop briefs the rest, so a build that took a single tool round used to reach the composer a second time with no request and no grammar in front of it (#477). Since 1.4.1 a container level addresses its children: the six doors an organisation or a member gets carry `context.org` / `context.member` beside the lane, permissively, so a second member of one organisation is a second address rather than a second delivery -- an unguarded lane and an address look identical while a container holds exactly one child, which is why it was never red (#478). Since 1.3.3 the round table also writes down the DOOR -- `build_caller` and `build_auto_submit`, the two keys the shell stamps at its rim -- and parks its caller row whenever the context names any of the six rather than only a tool_call_id: an operator has a door and no tool call, so the one build whose answer could not find its way home was the one build that wrote nothing down (#480). Since 1.4.0 the briefing publishes the FORM of `add_templates` and the loop's round budget: the catalogue carries a blank single-cell template for those four ordinary cell types now (`clock`, `fetcher`, `scriptlet`, `shelf`) and the head names them, so a wish that fits one is answered by NAMING it; a wish that fits none is answered by WRITING that class and instantiating it in the SAME manifest -- `add_templates` runs first in its diff -- instead of by seven rounds of searching for a template that does not exist, which is what a measured run spent before ending on `no_manifest_in_answer`. Whether an authored cell is accepted stays the submitter's `code.author` question and not the prompt's (#482). Since 1.4.2 that same block names the root `config.json` as an obligation instead of "one config.json per cell": a registered class whose cells live only in subdirectories has nothing for a node to be an instance of, and the refusal it used to earn one phase later named the NODE and the class the same manifest had just registered (#509). Since 1.4.3 a grown LEVEL declares itself AT the container it grows into -- scope `/os/orgs` for an organisation, `<member>/assistants` for an assistant -- and names its child bare, which grows the same tree with the same absolute edges but asks the broker for the scope root the shipped `colony.mutate.default` rule already permits: the FIRST organisation of a colony was the one build no front door could carry, and it is now a wish like every other (#503). Since 1.5.0 the hive path takes a fourth lane and it is pure TRANSIT: `in_ingest` goes straight to the librarian this template refs, the reconciliation report comes back out on `catalogue`, and the two are paired in `required_drains`. The corpus is a SEED, so a class registered after the colony was born is resolvable at the mutation door and invisible to the composer -- and the one caller who knows both facts, the submitter, stands OUTSIDE this hive, so the lane exists at the hive path or not at all (#504). And a grown CHANNEL is told the round it speaks in instead of inventing it: the member's identity used to be read off the last segment of the scope -- the DIRECTORY name -- which is right only while the folder happens to be spelled like the person, and where it was not, the audience gate refused every row of that person's memory in every leg and the bundle read exactly like an empty store. `ctx.member_person` is now required for the level, the claim stands in the rendered manifest, and a wish that does not name the person is asked rather than guessed at (#517). And a grown channel promotes TWO keys where it promoted one: `context.channel_node`, the node name the answer edge is guarded on, and `context.channel`, the chat id the session keeper opens a generation for, the firewall rate-limits and the memory writes down as the room -- one word could not be both, and as the node name it gave every chat of one connector a single session generation, a single rate bucket and a single room (#522). Since 1.5.1 the briefing carries the ROUND TRIP rule: a cell that asks and gets the answer back on the same lane tells the legs apart on a field of the ANSWER and POSITIVELY -- `hop` is single-hop and is replaced on every emission, `context` is persistent and is therefore identical on both legs, so a two-phase exchange over a store guarded on `context.phase` re-enters its own write branch and writes until the TTL (#521). Since 1.6.0 an assistant can be grown with the CREDENTIAL lanes beside it, the second opt-in of that level after the identity door: `credential` renders four more v-lanes -- two per brain, from the rim that spends the key straight to the member's own `access` -- plus the grants that answer them and, on both brains, a grant handle and an EMPTY `api_key`, which is the switch and not tidiness. It is a SECOND declaration, because the lane ends two levels inside the node the first one gives birth to and a mutation reads a newborn's contract from the template root only (#560). |
| [`builder-librarian`](builder-librarian/) | 2.1.1 | Lexical retrieval over the builder's knowledge base, as one sealed hive: a `store` cell holding the corpus in an FTS5 table, a `retrieve` cell that turns a request into a BM25 query and the rows back into a briefing, and -- since 2.1.0 -- a `catalogue` cell that reconciles that corpus against the colony's own template registry. No embeddings and no model -- a corpus that answers to names is answered by names. Retrieval is an ENHANCEMENT: a store failure comes back marked `degraded` rather than as an honest-looking zero-hit, so a corpus outage cannot hang a build. Since 2.0.9 a CATALOGUE lookup can also say no BY NAME: it asks the corpus for its template names in the same bundle as the search, and an unnamed request comes back as `no template by that name` plus the whole roll of names, with the hits below it marked as nearest neighbours -- BM25 always answers with its best rows, and a caller that cannot tell *not found* from *found something adjacent* has no reason to stop asking (#482). Since 2.1.0 it can also stop describing a library that moved: the corpus is a SEED, loaded once when the store's `cell.db` is created, so a class registered afterwards is resolvable at the mutation door and invisible to the composer (#496). A nudge on `in_ingest` asks `/colony/templates` what the library holds and writes one row per name the corpus lacks -- only ever adding, never touching a seeded row, and saying on the row itself that the registry answers an identity and not a declaration. Since 2.1.1 a catalogue row also carries a `PARAMS --` line beside `CONTRACT --` and `STORES --`: what an `override_params` may name, per cell, flat for a single-cell template and keyed by cell path for a subtree -- the third demand the mutation door enforces and the corpus did not publish, which cost a design lane three refusals on a set of two names (#505). **Its seed is a build product, and the published one is not the one a development tree carries**: the corpus shipped here is generated from the public sources alone (the spec plus this table's templates), because a corpus is a copy of its sources and this repository's are not all public (#441). |
| [`canvy`](canvy/) | 2.1.10 | An interactive canvas of the colony, on a port of its own: a timer takes a topology snapshot, a `probe` asks `/colony/graph`, a `layout` cell turns the snapshot into display objects, and a `web` cell holds them and serves the page. Since 2.0.0 canvy draws nothing itself -- it is the first application of the `web` cell, and node positions are `editable` object props, so a drag is local CRUD plus a diff to every viewer instead of a topology round trip. The browser owns the drag and the camera, nothing else. Since 2.1.8 a hand-placed cell can be handed back to the layout: the pin is a marker of its own (`pinned`), not the mere presence of a coordinate, and the detail panel releases it. Ask it for a fresh snapshot on `in_refresh`; a semantic browser event leaves on `event`. **Deprecated since [#455](https://github.com/mmeyerlein/meclaw/issues/455)**: it fuses a surface and a view, which are `display` and `colony-view` below. Nothing is removed and no instance breaks -- instantiation copies -- but a new screen is a `display`. |
| [`clock`](clock/) | 1.0.0 | A periodic tick a manifest can name, as one `timer` cell: an interval and a schedule key, both parameters, and nothing else. It exists because of a gap in this library rather than in the substrate -- nine shipped templates carry a timer INSIDE them and not one of them was instantiable on its own, so a running colony could not be given a tick by any manifest, through any door (#484). It emits and it decides nothing: the body is empty, no lane is named, and `emit_to` is `.` -- the parent draws one edge off it and stamps the lane it wants, which is what lets the same template drive a sweep in one place and a trim in another. The cadence is a parameter three ways: `override_params` at instantiation, the `CLOCK_CRON` knob colony-wide, and an `op` message at runtime. No catch-up, no condition, no store: a clock that knew what it was ticking for would be a second scheduler. |
| [`cogny`](cogny/) | 4.6.0 | The agent core as one node: a tool loop of `collector` + `dispatcher` around an llm brain slot, every internal edge pre-wired. A talky without a channel. Asked on `in_turn`; answers on `answer`, calls tools on `tool`, asks a memory on `recall` and drains a failed inference on `error`. Since 4.2.0 it takes an identity on `in_pack`, answering once on `pack_ack` (#458). Since 4.3.0 its menu is ASKED FOR rather than typed: it declares `["*"]`, a `menu-clock` ticks, the names leave on `schemas` and the declarations come back on `in_menu` into the brain as durable `system.tools` (#464). Since 4.4.0 it is the PROBLEM SOLVER and only that (#528): ONE brain -- `brain_fast`, `escalate_to_deep` and the `consult_class` lookup lane are gone, because a fast memory question belongs to the surface that holds the window -- it DECLARES ITS OWN ERRAND on `in_schemas` / `tool_schemas` in the tools hive's own shape (one schema, `consult_cogny`, with `question` and `context` both required, and its description is the class boundary), its `in_turn` lane requires `context.session_id`, and its memory is a TOOL (`memory_call_tier` "1") rather than an ambient bundle. Also in 4.4.0 the "without a channel" is enforced rather than stated: the dispatcher ships with `interim` off, so the sentence a thinking model puts next to its tool bundle no longer reaches the asking voice as an advice it never gave (#539). Since 4.5.0 the cell that answers `in_schemas` is called `schemas` and not `declare` -- one lane, one cell name, the tools hive's own word, so an `override_params` path and a mutation that adds a schema stop depending on which of two words a hive happened to pick (#548). Since 4.6.0 it declares the same credential connect point `talky` does -- `emits.credential_request` and `accepts.in_sealed`, both `at: ["./brain"]` (#560) -- so the reasoning core authenticates out of the person's vault rather than out of the environment. |
| [`collector`](collector/) | 3.4.0 | Context assembly as a hive: a rolling conversation window, the memory bundle and the tool round fanned back in -- each leg capped by configuration, not by a model's judgement. **Since 3.0.0 route `turn_write` hands out one message per turn**, not one batch of the day: a caller wired to the batch shape breaks (#298). Since 3.0.1 every junction of the assembly is ONE bundle instead of a fan-out with a guarded fan-in: opening a turn costs one store message instead of three, an assembly one instead of four, a tool round one instead of four (#419). Since 3.2.0 it carries `in_pack`, the ONE lane through which durable `system.*` state -- `identity`, `persona`, `handover` -- reaches the brain of a sealed agent composite from outside (#458). Since 3.3.0 the tool menu is asked for instead of typed: `params.tools` is what the agent DECLARES it uses, `in_menu_tick` sends those names to a tools hive on `schemas`, and the answer leaves on `menu` as durable `system.tools` -- one write per change, nothing per turn, and `tool_menu` stays as the manual override (#464). Since 3.3.1 that menu also carries the two tools this hive answers ITSELF -- `memory_recall` and `thread_recall`, whose declarations used to ship as brain seed rows the first `$replace` deleted, leaving a grown agent with a wired recall chain and no way to ask (#512). Since 3.4.0 the menu is a UNION over ANSWERERS rather than one answer: one reply was the whole menu, which is right while exactly one thing answers and is the whole defect the moment two do -- the second would not merge with the first, it would delete it. Each answerer's last submenu is one row of this hive's own store, keyed by `context.tool_answerer`, and every answer re-derives the union; `menu_unknown` is computed against the merged menu, and a store that refuses the merge is a warn line rather than a `degraded` answer with no turn to be an answer to (#529). |
| [`colony-view`](colony-view/) | 1.0.1 | The colony as one view, and the library's first **app**: a timer, a `probe` that asks `/colony/graph` and a `layout` that turns the snapshot into a component tree. It owns no port and no display -- it emits ONE view towards a screen and declares the components that draw it in the same body, prefixed with its own name so two apps on one screen cannot collide. The FLOW owns where a box sits and says so on every tick; a hand owns an offset beside it (`hand`, ONE prop spelled `"dx,dy"`, because a browser writes a prop at a time and two props reached the page as two pictures) and a `pinned` marker, and those two the node declares `keep` -- so an arrangement survives every tick while everything nobody has touched stays exactly where the layout wants it. Since 1.0.1: before it, `x`/`y` themselves were kept and a coordinate WAS the pin, so a box froze at the tick its object happened to be created and 208 of 215 unrelated hive pairs ended up with overlapping frames ([#544](https://github.com/mmeyerlein/meclaw/issues/544)). A box is identified by its cell path rather than by its slot in the tree. The offset is NOT bounded: a bound was tried and withdrawn, because trimming an offset to its own hive's frame left a box 85 x 15 screen pixels of travel and read as a broken gesture -- so an arrangement CAN put two frames over each other, and what keeps the picture honest is the marker rather than a wall. Tagged `app` in its `template.json`, which is the whole of what an app is: a composition that produces views and is instantiated under `<member>/apps/<name>`. |
| [`daily-digest`](daily-digest/) | 2.0.3 | Scheduled fetch-and-forward as one hive: a timer fires daily, a `web_fetch` pulls one URL, a `code` cell formats what came back, and a Telegram `proxy` delivers it to a fixed chat. Five cells, no model. A parent may also demand a run at the hive path on lane `in_digest`, bringing its own `context.chat_id`, and take the formatted digest back on lane `digest` — so the same template is both a standing schedule and a callable step. |
| [`dispatcher`](dispatcher/) | 1.1.2 | The fan-out half of a tool loop: splits a brain's `tool_call` bundle into one routable message per call, announces the round to the fan-in, and passes a final answer through. Since 1.1.2 the sentence that stands next to a bundle leaves only where there is a channel to say it in: `params.interim`, default on, turned off by a brain whose answer lane feeds another agent rather than a user (#539). |
| [`display`](display/) | 1.0.1 | One screen as a hive: a `web` cell on a port of its own, a `views` store and a deterministic `compose` cell that lays the views out. It is a CHANNEL, so it belongs to a person rather than to an agent -- several agents hold views on one screen at the same time, and none of them can touch another's, because the owner of a view is the envelope's `reply_to` and never anything the body claims. A view is prose or a component tree; the prose one is the smallest view there is and needs no app at all. Since 1.0.1 a tree node may name its own `key`, and the object it becomes is named by that instead of by its child index -- an index is a slot, and a prop somebody asked to `keep` across ticks has to follow the THING rather than the slot ([#544](https://github.com/mmeyerlein/meclaw/issues/544)). Nothing here asks a model: one region, newest first, and an elapsed `ttl_ms` is not laid out. Ask it on `in_view` / `in_withdraw`; a browser interaction leaves on `event` and a refusal on `receipt`, both naming the owner they belong to. |
| [`door`](door/) | 1.0.2 | The first cell of a colony, as one code cell: it takes what the HTTP ingress delivers -- request headers in `context`, an empty hop -- and puts the turn on a named lane, carrying the channel identity with it. |
| [`fetcher`](fetcher/) | 1.0.0 | An outbound HTTP GET a manifest can name, as one `web_fetch` cell: a timeout, a size cap and an egress policy, and no target. It exists because of a gap in this library rather than in the substrate -- three shipped templates carry a `web_fetch` INSIDE them and not one of them was instantiable on its own, so a running colony could not be given the ability to read a document off the network by any manifest, through any door (#482). The URL rides in the `tool_call` args on the wire, because that is where the cell reads it: a fetcher with a target baked into its config would be a fetcher for one caller. GET only -- method, request headers and a request body are a core defer of the cell type, and a template cannot ship what the cell does not have. The egress policy is the cell's own and default-deny: `web_fetch` runs in the daemon process, so `sandbox.network` can never cover it, every redirect hop is re-checked before the connect, and link-local stays shut in both steps of the opt-out. A non-2xx answer is a NORMAL result carrying `hop.http_status` -- what to do about it is the edge's decision. |
| [`firewall`](firewall/) | 2.2.0 | Deterministic screening in front of an agent: size, sender, forbidden literal and rate, each verdict naming the rule that fired. Nothing here asks a model, and nothing here can. Since 2.1.0 the rule table travels: `in_export` hands it out as a versioned document and `in_import` takes one back, so a rebuilt colony screens its first turn against the policy the old one ended with -- the spent rate window deliberately stays behind (#471). Since 2.2.0 two refusals are no longer rows: a hardline layer lives in the template's code, is consulted ahead of the table and can only ever reject, so emptying the rules does not open it (#449); and a third verdict `hold` parks a turn whole until a person releases or refuses it, or its timeout runs out -- every turn still ENDS on `pass` or `reject`, a held one just ends later (#450). |
| [`meclaw-os`](meclaw-os/) | 1.7.0 | The colony shell, as the outermost composition level: the capability broker (`access`) and the control loop (`argus`) every organisation shares, plus one open, empty container the organisations are instantiated into. Thirteen lanes in, fifteen out, forty-nine edges, and no cell of its own. Since 1.1.0 it also holds the two halves of the one authoring path a colony has: the `builder` that drafts a manifest and the `submit` that carries it to the mutation door (#425, ADR-0015). Since 1.2.0 those two are wired to the broker as well: the submitter asks whether a manifest may be applied, and only this level can draw that pair (#435). Since 1.3.0 it also holds the `operator` -- the one front door a person addresses the OS through, and the only thing in the tree that gives a POST a sender path (#446). Since 1.3.2 the control-loop occupant is `argus`, renamed from `steward`, and one more lane leaves the level with it: `alert`, a watched symptom the loop counted deterministically without asking a model -- nothing here can act on it, so the shell draws the exit and hands it out (#462). Since 1.5.0 the thirteenth inbound lane is `in_build`: an operator can ask the baumeister for a draft, and the draft comes back to the front door as a submission rather than into a container that is empty by construction -- without it the FIRST build of a fresh colony had no caller (#469). Since #474 that draft STOPS there to be looked at: `in_build` reads `hop.auto_submit`, and without it the manifest reaches the front door on `in_draft`, is parked under its own digest and answered, and is applied only when the operator quotes that digest back. Since 1.6.0 the shell nudges the corpus it changed: `./submit -> ./builder` on a receipt with no `error_code` and `hop.registers_class`, re-stamped `in_ingest`, with `./builder -> .` carrying the report out on `catalogue`. Only this level can draw it, for the same reason it is the only one that can draw `./submit -> ./access` -- the two are siblings here and nowhere else. Forty-eight edges then, thirteen lanes in and fourteen out (#504). Since #533 one more lane leaves: `bundle`, the answer to a question asked at a member's own `in_recall` door -- the question has crossed this level since it was written and had no answer lane at any of the three levels it has to leave. Since 1.7.0 the shell holds FOUR refs and not five: `submit` moved inside `operator`, and with it the six edges that carried a submission across two shell stations for one job -- `./operator -> ./submit` and `./submit -> ./operator` are interior to the front door now, while the ask, the grant, the mutate and the two corpus nudges are drawn from `./operator` with the same guards. Forty-seven edges; the declared lanes are unchanged, because the union rule cancels exactly what moved inside (#556). |
| [`member`](member/) | 1.5.0 | One person, as a composition level: the memory (`memory-hive`), the curated record (`affinity`), the screen (`firewall`) and -- since 1.3.0 -- the CHANNELS every assistant of that person shares, plus -- since GH #459 -- the APPS that write onto those screens, and three open, empty containers the assistants, the channels and the apps are instantiated into. Seven lanes in, twelve out, fifty-two edges. Two assistants of one person must know the same person, must meet one attacker and must be reachable on the same bot, which is why all four stand here and not one level down (GH #122, #454). A channel is ONE node in `./channels`, named for what `context.channel_node` will say -- the address the answer is routed back by, split off from `context.channel`, the CHAT the turn is in, in #522, because one word keyed both the return edge and the session generation and every chat of a connector therefore shared one; the turn it raises is stamped with `context.assistant`, and one edge per assistant in `./assistants` reads that name -- a sum, never the cross product, because an edge target is static in this substrate. A SCREEN is such a channel (`display-<screen>`, holding a `display@1.0.1`) and needs no lane of its own on the way down: an agent's ordinary `answer` is the smallest view there is, and the channel's own down-edge re-stamps it to `in_view`. What only a screen produces is `event` and `receipt`, and both come back addressed by the OWNER the display stamped on `hop.owner` -- the path of the cell that put the view up. The level splits on the container (`/assistants/` becomes `in_turn` with `hop.kind`, `/apps/` keeps its lane, anything else leaves on `error`); which agent and which app is the mutation's edge, exactly as with the channel. Since 1.2.0 it is also the level that FIRES the memory's close pass -- a closed session's write batch is fanned onto `in_close_pass` as well as out of the level, so the strong model reads the session whole (#447) -- and the level that lands an export: `in_export` starts a walk in EVERY holder -- memory, record and screen since #471 -- and one cell of the member's own, `export-sink`, files each part under the hive it came out of as `<hive>/seed/<table>.jsonl` under `MEMBER_EXPORT_DIR`, with a marker per hive and one for the member that the completion word `export_done` stands on. Three travel per export, one per holder -- FOUR when the caller names a generation on `context.assistant`, because since #475 the walk also reaches the session keeper inside it, four levels down, and the sink files its ledger in a fourth directory. The generation is NAMED rather than fanned to: two generations hold two ledgers, they are not one document, and both would claim the same directory. Since #533 the level answers the question it takes at `in_recall`: `bundle` is an emits lane, guarded on the reply-to token this level's own door stamps (`'outside'`, ADR-0019), and the way down into `./assistants` is the `default` -- so an asker inside a generation is served exactly as before and an asker outside gets an answer at all. A refused recall is sorted the same way, and the one raised inside comes back to its round as `in_bundle` instead of being waited out. Since 1.5.0 the level also holds an `access` of its own (#560): authorisation and key ownership are two different jobs, and the provider keys this person's brains burn belong to the PERSON rather than to the OS -- the same argument that puts the memory here. Nothing in the member's graph reaches it but the drain for its `error` lane; the two edges that carry a credential are V-LANES (#559) drawn onto `./access` by the manifest that grows a generation, straight from the brain that spends the grant, and README section `The credential v-lanes` is the form. Since 1.5.0 it also SAYS that about the memory road: `recall` and `in_bundle` are declared here as lanes of the level rather than of its rim -- `at` names `./assistants` and nothing deeper -- so a v-lane that would carry a generation's recall past the door that stamps `audience_now`, `channel` and `recall_as_of` is refused `v_lane_mandatory_hop` at mutation time instead of becoming `missing_audience` at runtime (ADR-0020, #562). The stamping edges themselves are untouched. |
| [`memory-drain`](memory-drain/) | 2.0.6 | The adapter between a write batch and a central memory: decomposes one batch into single-turn episodes, in order, idempotent across replays. Since ADR 0012 an **import adapter for foreign history, wired by nobody** — a live conversation writes its turns one message at a time and never passes through here. |
| [`memory-hive`](memory-hive/) | 3.1.0 | A member's long-term memory as a hive — one source of truth that every agent of that member reads: an LLM-free write path, a token-budgeted tier-0 bundle, a four-leg retrieval fan fused without a model, and a nightly consolidation that supersedes instead of deleting. Thirteen cells, no Rust. Since 2.2.0 the remembered content can also leave the hive as a versioned document and enter another running one, idempotently -- the template-level answer to #243, while #253 is the substrate one. Since 2.3.0 a tier-1 recall delivers two documents in one message: a bundle written for the model that has to answer -- provenance, currency and an explicit "nothing here answers this" -- and the retrieval's own record beside it, for whoever has to explain the answer afterwards. Since 2.3.4 a tier-0 recall costs ONE store round trip for its three legs instead of nine, and parks nothing; since 3.0.2 a tier-1 recall costs SIX instead of 47, measured, with the answers byte-identical across the rebuild (#418). **Since 3.0.0 there is exactly ONE lane that writes facts mid-conversation** — the answering model annotates the turn it just answered; the batched `extractor`, its gate and the `in_flush` lane are gone (#298). Behind that ingress stand two readers and no second writer: the night, and a close pass that reads an ended session whole on a strong model (`in_close_pass`, #300). Since 3.1.0 a German question reaches the index it is asking — the recall lane CUTS words on the Unicode word class and leaves the folding to the tokenizer that owns it, where the old ASCII class made `ae`/`oe`/`ue`/`ss` separators and cut the German word for *sons* down to `hne` (#518); an EPISODE carries a vector, so the semantic leg is no longer a fact-only leg (#519); and the graph leg joins the facts of the nodes it walked, so from an entity there is a path to a fact rather than only to the provenance of an edge (#520). |
| [`operator`](operator/) | 1.1.0 | One front door into the OS, as a sealed hive: one occupant per subject -- `intake` turns a posted manifest into a message that has a sender, `export` triggers a dump of a member's memory, `lifecycle` composes a birth, a sleep or a wake -- plus a fourth that turns a lane nobody wired into an ordinary receipt instead of a dead letter. What it adds is one thing: IDENTITY. The substrate stamps `reply_to` on a CELL's emission, so an agent inside the colony is attributable and a person with a shell is not; a request that becomes a message emitted by an occupant of this hive has a path, and operator mutations stop being anonymous in the mutation log. **Never authentication** -- no token, no header check, no caller list: who may reach the door is a reverse proxy's question and what they may do is the broker's. It decides nothing, and sleep and wake are `add_edges` / `remove_edges`, because the substrate has no activate operation and this template invents none (#446). Since #474 it also holds a draft still: a manifest the baumeister drew for an operator arrives on `in_draft`, is parked in the `drafts` store under its own digest and answered with a receipt naming it, and NOTHING is applied until the operator posts `in_submit` quoting that digest. The one thing this hive remembers, and it is a proposal rather than the state of a request. Since 1.1.0 the SUBMITTER lives here too, as the `submit` occupant: one front door and one place a submission lives, instead of two shell stations for one job (#556). Three lanes crossed the rim with it -- `ask` out and `in_verdict` back to the broker beside this hive, and `mutate` out to a door that is birth topology or nothing -- and the cell that was called `submit` through 1.0.0 is `intake`, because a ref is named after the template it references (§ 8a, R1). |
| [`org`](org/) | 1.3.0 | An organisation as a composition level, and as nothing more: a name, a boundary and one open, empty container its members are instantiated into. No cell at all -- seven transit lanes in, twelve out, nineteen edges. A level that shares nothing is still a level when what it is worth is the namespace. The twelfth lane is `bundle` (#533): `in_recall` crossed this level from the day it shipped and its answer had nowhere to go. |
| [`receptionist`](receptionist/) | 2.0.5 | One agent per channel, built on demand: the first turn of a channel nobody has met instantiates a fresh `talky` for exactly that channel and hands the turn straight into it. |
| [`retry`](retry/) | 1.0.0 | A bounded retry loop around one tool, as a single cell. At the cap the give-up lane hands the last error on with its `error_code` intact. |
| [`scriptlet`](scriptlet/) | 1.0.1 | A script a manifest can name, as one `code` cell shipped BLANK: a runner, a timeout and a pass-through, and no purpose of its own. Five templates here hold a single `code` cell -- `door`, `terminal`, `retry`, `dispatcher`, `archive-bridge` -- and every one of them carries a script and a job, so a composer that needed a plain one had nothing to name and spent seven rounds looking (#482). The logic arrives as `script_inline` in the `override_params` of the declaration that instantiates it; the shipped script hands `body.messages` straight back, so an instance without an override boots and passes turns through rather than failing until repaired. `contract.multi_send_capable` is declared, because a blank `code` cell that could only ever emit one message would be half a cell and a `contract` is not something an override can reach. It carries no `params.sandbox` key either, so no manifest can widen the default-deny profile through it -- and an `override_params` bearing a script is executable behaviour arriving with a manifest, which is the `code.author` question the submitter asks its colony, disabled by default. Since 1.0.1 the row also publishes the WIRE the code it carries has to meet: a `SCRIPT` line beside `PORTS` names the three stdin objects and where the payload sits (`doc["body"]["messages"]`, and a turn carries `text`, never `turns`), where the two header compartments live, that stdout is body slots and that a `{"content": ...}` comes back `contract_violation` -- plus two worked scripts, a pass-through and a transform. A composer does not read a README, it reads this row, and the one template whose purpose is to carry authored code published the least about the contract that code has to meet (#513). |
| [`session-keeper`](session-keeper/) | 2.1.0 | A session as a channel generation, modelled on a phone call: minted at the surface, stamped onto every inbound turn, ended by arithmetic (a timer plus an idle threshold) rather than by judgement. Since 2.1.0 the ledger travels: a keeper that receives another's document CONTINUES the conversation the source had open instead of greeting a year-old acquaintance as a stranger (#471). Since #475 a member's own export reaches it, through `talky` and `assistant`, when the caller names the generation it stands in. |
| [`shelf`](shelf/) | 1.0.2 | A place to put rows a manifest can name, as one `store` cell: one declared table and a query timeout, and no opinion about what goes on it. Eighteen shipped templates carry a `store` INSIDE them and not one of them was instantiable on its own, so a running colony could not be given somewhere to keep what it produced by any manifest, through any door (#482). The shape is the parameter that makes one template two shelves: `params.schema` is a bootstrap declaration, so an `override_params` at instantiation is baked into the `cell.db` by DDL before the cell ever wakes. Constraints are not shippable -- no PRIMARY KEY, no UNIQUE, no NOT NULL is a core defer of the cell type -- so two identical rows are two rows and dedupe belongs to whoever writes, as a stable id computed before the insert. `params.fts`, `params.canonical` and `params.write_surface` are deliberately absent, which also puts them out of an override's reach: a store that needs one of the three is a template of its own, and writing it in the same manifest is what `add_templates` is for. Since 1.0.1 the row carries an `ARGS` line for the same reason one cell downstream: which keys each operation reads, that an unknown one is refused rather than ignored, two worked calls, and what a read's turn text IS -- a bare JSON array of row objects, never `{"rows": [...]}`; a verification run wrote both scripts to the published contract and then lost forty legs to an argument name the row did not name (#513). Since 1.0.2 the row also publishes its RETURN lane: `hop.operation` is the only field on an answer that names the leg it belongs to, a return edge is guarded positively on it, and the leg no guard takes rides a `"default": true` edge instead of the dead-letter queue (#521). |
| [`steward`](steward/) | 2.0.13 | The colony's control loop: charter, deterministic measurement, a judge that simulates before it decides, a params update to the cell it names, an immediate health check, and keep-or-revert after the window -- every cycle a receipt. Ships with every goal disabled. **Deprecated since [#462](https://github.com/mmeyerlein/meclaw/issues/462)**: it is renamed `argus` above; nothing is removed and no instance breaks -- instantiation copies -- but a new control loop is an `argus`. |
| [`submit`](submit/) | 2.3.0 | Two occupants behind one door, and the only reach onto the mutation door in the whole tree. It checks that a manifest's bytes are the ones its digest was drawn over, takes the requester's identity off the ENVELOPE rather than out of the body -- and then ASKS: who may submit is a check-only question to the capability broker, put once per submission over the manifest's scope root, while the manifest waits parked in the store beside the gate. A permitted manifest is un-parked, stamped with its attribution and emitted once. The colony's answer comes back directly and becomes a receipt that carries the id of the call that asked for it, because the same store also remembers the round in flight. Since 2.1.0 there is a SECOND question, and the gate derives the need for it from the diff rather than from a prompt: a manifest carrying a `script_inline`/`script_path` override or an `add_templates` is asking to author executable behaviour, so `code.author` is asked over the same scope root and a missing rule refuses it with `code_author_denied` (#446). Since 2.3.0 the receipt also says what the DIFF asked for and not only what the door did: `hop.registers_class`, `true` on every receipt whose manifest carried an `add_templates` and `false` on every other one. It is REMEMBERED on the flight row rather than read off the answer -- the colony replies on a fresh trace that carries no diff at all -- so an edge can test for "this submission registered a class" without opening a body (#504). |
| [`summarizer`](summarizer/) | 2.0.2 | The handover step: when a generation closes, it folds the day's write batch into one recency-weighted summary and emits it as a `system.handover` update. **No longer part of `talky` since talky 4.3.0** (#447): a closed session is now read whole by the memory hive's close pass, and handover comes back through the recall bundle instead of through a slot in one generation's own prompt state. |
| [`talky`](talky/) | 4.6.0 | The full composite agent: `session-keeper`, `collector` and `dispatcher` around an llm brain slot, with the loopback and the close path already wired. Since 4.3.0 it carries no `summarizer`: the close batch leaves on the write port and the handover for the next generation comes back through the memory recall bundle (#447). Since 4.1.0 a `splitter` sits on the answer path and cuts the extraction sidecar out of an annotated answer onto its own `extraction` lane — per-turn memory extraction stopped being a tool call (#379). What ASKS for the block is the collector inside the same composite, on `instructions.sidecar` and on every assembly: the contract is a promise of this template and not of a person's charter, and until #525 nothing delivered it at all, so the whole lane ran wired and inert while the memory stopped growing. Since 4.4.0 the composite has a door for its own identity: `in_pack`, paired with `pack_ack` (#458). Since 4.5.0 it also stops carrying a tool menu somebody typed: the collector's `params.tools` names what this agent uses, a `menu-clock` ticks, and the declarations come back from a tools hive on `in_menu` (#464); and it passes the keeper's own transfer lanes through -- `in_export` and `in_import` in, `dump` out -- so the sessions stop being the one table a rebuilt member came back without (#475). 4.5.1 re-points the collector ref at the merged menu (#529); standalone this composite has one answerer and behaves as 4.5.0 did. Since 4.6.0 the rim names `./brain` as the connect point of the credential lane -- `emits.credential_request` and `accepts.in_sealed`, both with `at` (#560/#559) -- so a member can wire this brain to its own broker in ONE edge per direction instead of a pass-through chain through three rims, and the brain can run on a grant with no key in its config. `params.ports` stays `[]`: a v-lane is the one opening a template pronounces about itself. |
| [`telegram-connector`](telegram-connector/) | 2.0.1 | A Telegram chat as one address: one `proxy` cell that owns the chat credential, taken verbatim from `bot-basic@2.0.0`. A turn in from the chat and a finished answer back, both on the cell itself; `hop.error_code` tells an inbound turn from the connector's own failure, and the level that holds it owes that failure a drain. No persona, no model, no answer of its own. Since #515 the cell shows a typing status while the topology behind it works: `sendChatAction` the moment an inbound update is accepted, repeated until the answer for that chat has gone out or a ceiling is reached, and nothing is written into the chat -- a connector that posts "still working" has changed the transcript the agent reads back on the next turn. The behaviour is the substrate cell's, so the version did not move. |
| [`vault`](vault/) | 1.2.0 | A secret store with no operation that returns a secret -- not a policy over a store, but a cell type whose route surface has no read on it. Secrets enter over the user channel only; the broker may ask it to USE one, or to DELIVER one sealed. A vault behind a sealed hive opens itself from an environment variable it names, or not at all. |
| [`terminal`](terminal/) | 1.0.1 | The last cell of a lane, as one code cell: it accepts anything and emits nothing. Its whole job is to be an address, so that a lane without a destination yet still HAS one -- the message arrives, the trace records it, and the dead-letter queue stays empty. |
| [`tools`](tools/) | 1.4.0 | The tool surface of one assistant as ONE node with ONE contract: `tool_call` in, `tool_result` out. Sealed (`params.ports: []`), so which tools exist is a change INSIDE the hive and never a change to the caller's edges -- seven tool occupants today (a sandboxed one-shot shell, a GET-only fetcher, a generic search wrapper, a filesystem reader and an in-place editor sharing one root, and the two halves of a structural build round), plus a cell that turns an unknown tool name into a named refusal and one that hands out the schemas. Replacing the tools with one code-executing cell is still a single `swap_nodes`. **`tool_result` is still the one RESULT lane, whatever the tool was**; `build` is a different class -- the REACH of the surface, declared in the contract rather than discovered in an edge table (#425). Since 1.2.0 the hive also hands out the SCHEMAS: a caller names the tools it uses on `in_schemas` and gets their declarations back on `tool_schemas`, with a name nobody wired reported as `unknown` rather than dropped -- so adding a tool stops meaning editing every caller's prompt by hand (#464). Since 1.3.0 every occupant is REACHABLE: the `mcp` and `vault` directories that stood here unwired are gone, because a cell type is not a tool -- a vault answers its broker and stands in the broker's own hive, an `mcp` bridges one named server and is wired when somebody names it, and an occupant nobody can reach teaches nothing (#547). Since 1.4.0 the two halves of the build round are NAMED as a pair: `build` and `apply` are `build-draft` and `build-apply`, because one fetches a draft and the other submits it, and the shared prefix is what makes the pair visible in the graph and in a registry listing. The tool names the model sees are untouched -- `build_topology` and `apply_manifest` are the contract surface, and a cell name is not a tool name (#554). |
| [`web`](web/) | 1.1.0 | A display as one cell with a port of its own: an HTTP + WebSocket listener, an object tree and a component library in its own `cell.db`, and server-side rendering that is materialised -- a page load answers from memory and costs no cell call. Components are rows, so a model can define one at runtime. Ships the Vision design language as seed data: the token stylesheet plus nine components (`stack`, `card`, `heading`, `text`, `table`, `button`, `input`, `badge`, `ornament`) and a `/demo` page composed of nothing else. Two of its rules are refusals rather than advice -- glass is a navigation-layer material, and glass never sits on glass. Authentication is external, forever: the default bind is loopback and a reverse proxy goes in front. |

Read a template's `template.json` before wiring it -- the `description` slots (`purpose`,
`use_when`, `not_in_scope`, `examples`) say what it is for, and for a hive its `params.contract`
says which lanes it accepts and emits, which is what you need in order to connect it. `cogny`
and `talky` are composites: they build on the smaller templates as sub-units, so instantiating
one of them pulls in nothing that is not in this table.

`meclaw-os`, `org`, `member` and `assistant` are the four **composition levels**, authored
under one rule -- *a level owns what its siblings must share* -- and each of them is a
composite too: `ref`s to templates that already stand in this table, its own topology, and the
open, empty containers the level beneath it is instantiated into -- one each for `meclaw-os` and
`org`, three for `member` (`assistants`, `channels` and `apps`), and none at all for `assistant`, which has
been complete at birth since #454 moved the channels up to the person. Instantiating the outermost
one and then filling the containers downward grows the whole stack, which is what
[`examples/organism`](../examples/organism/) does with five declarations and no hand-written
interior edge.

A directory name inside a template is the address every grown instance answers to, so an
app-hive name that collides with the substrate glossary is a review defect, not a
preference: name it `catalog`, not `registry`, and `calendar`, not `scheduler` (GH #26).
`crates/meclaw-cells/tests/gh26_app_hive_names_avoid_the_substrate_glossary.rs` refuses
those two words across the whole tree -- deliberately two words wide, because a gate with
an exception list is one authors route around, and a name is cheap to change now and
expensive after the first instance is grown.

## Instantiating one

Point a running colony at this directory and send an `add_nodes` mutation. The short form:

```bash
./target/release/meclaw --root ./mycolony --templates ./templates \
                        --daemon --api 127.0.0.1:7777

curl -s -X POST http://127.0.0.1:7777/colony/mutations \
  -H 'Content-Type: application/json' \
  -d '{"scope":"/","ctx":{},"diff":{
        "add_nodes":[{"name":"agent","template":"talky"}]
      }}'
```

Because a template has no outgoing edges, the node above lands connected to nothing -- and a
subtree that nothing crosses into derives inactive, so its long-running cells never spawn. Wire
it in the **same** mutation: one `add_edges` entry from an already active cell onto the
template's own path is enough to bring the whole subtree up on that one recompute.

And if you want it wired but NOT running -- a long-poll consumer whose upstream tolerates only
one reader, say -- declare `"birth": "inactive"` on the `add_nodes` entry: a cell born inactive
is registered, addressable and persisted inactive, and no task is built for it, not even when
the same mutation wires it. The next mutation that NAMES it wakes it -- and only one that
names it: the declaration is durable, so a mutation elsewhere in the tree leaves it asleep
however far its recompute reaches, and a restart brings it back the way it was ([#491](https://github.com/mmeyerlein/meclaw/issues/491)).

```bash
curl -s -X POST http://127.0.0.1:7777/colony/mutations \
  -H 'Content-Type: application/json' \
  -d '{"scope":"/","ctx":{"model":"openai/gpt-4o-mini"},"diff":{
        "add_nodes":[{"name":"agent","template":"talky"}],
        "add_edges":[{"from":"./ingress","to":"./agent",
                      "modifier":{"set_hop":{"route":"'in_turn'"}}}]
      }}'
```

The `ctx` block feeds the `${ctx.*}` placeholders a template declares -- `talky` wants a resolved
model literal.

**That edge is the finished shape** (§ The hive boundary). Since
[#228](https://github.com/mmeyerlein/meclaw/issues/228) every hive template that ships is sealed:
the address is the template's own path and the request is a lane on `hop.route`. Nothing here
names a cell inside a hive any more, which is what lets a template be swapped for another one
arranged differently. The lanes a hive accepts and emits are in its `params.contract`.

Working colonies built this way live in [`../examples/`](../examples/). Start with
[`hello`](../examples/hello/README.md) for the model itself, then
[`swarm`](../examples/swarm/README.md) for a tool loop. The mutation format is specified in
[`../docs/meclaw-overview.md`](../docs/meclaw-overview.md); the cell types a `config.json` may
declare are in [`../docs/cell-types.md`](../docs/cell-types.md).

## Versioning

A reference in a mutation is either `name` or `name@major.minor.patch`. With a version it is an
**exact** match (`talky@2.0.0`); without one, it resolves to **the one version registered under
that name**. **Correction (GH #277):** this line used to say "the highest version on disk wins".
That is dead under the uniqueness rule — the scan aborts as soon as two `template.json`s declare
the same `name`, *regardless of their versions*, so a scanned library holds exactly one entry per
name and a bare-name reference has exactly one answer. There is no "highest" to pick from.

Semver ranges (`^`, `~`) are not parsed today, so `talky@2` is not a resolvable reference -- when you see
`talky@2` in prose here or in an issue, it names the major line of the template, not a string you
put in a mutation.

Version numbers here move only forward, and a bump never reaches a colony that is already
running: instantiation copied the subtree, so a running instance is pinned to the bytes it was
built from. Upgrading is therefore always an explicit act -- instantiate the new version next to
the old one and move the edges -- never something that happens to you between restarts. That is
the reason the copy exists.

**Where pinning stops working today, and the intent.** A template lives in exactly one
directory, so a version bump **replaces** it: after `talky` goes to `2.0.0`, a mutation asking
for `talky@1.2.0` finds nothing and is rejected, even though that is precisely the request a
pin is supposed to survive. The pin protects a *running* instance (it already holds its copy);
it does not protect a *new* instantiation reproducing an old one. That asymmetry is a gap, not
a design. The intent: **starting with 0.9.0, superseded template versions remain available**,
so a pinned reference keeps resolving after the version it names has been superseded. Until
that lands, treat `name@exact-version` as a statement about what you built against, and vendor
the directory if you need to rebuild it later.

**Correction (GH #277) — how that promise has to be read now.** The obvious reading of "superseded
versions remain available" was: keep the old directory beside the new one
(`templates/talky-3.0.11/` next to `templates/talky-3.0.12/`, both declaring `"name": "talky"`).
That reading is no longer merely unsupported, it is a **scan error**: the uniqueness rule refuses
the second `template.json` declaring an already-seen name, regardless of version, and the whole
scan aborts — taking the boot or the `RescanTemplates` with it. The promise itself stands, but it
now needs a mechanism it did not need before: an archive or a registry that holds superseded
versions *outside* the one-directory-per-name library (a separate library root, a content store —
undecided), not a second directory inside it. Until such a mechanism exists, one directory per
name is the only shape a library may have.

## Env knobs are an experimental surface

Most templates here take their tunables as `${KNOB}` substitutions out of `.env`. That is
convenient and it is temporary: the knobs are migrating onto the `params` block of the cells
that read them, template by template, over the `0.x` line. Until a template's migration lands,
**its knob names are not a compatibility promise** -- a name may change, split, or disappear in
any `0.x` release, and the template README is the only place that says what it is called today.

The migration is tracked in
[#138](https://github.com/mmeyerlein/meclaw/issues/138); the `collector@1.2.0` migration
([#136](https://github.com/mmeyerlein/meclaw/issues/136)) is the reference pattern, and every
migration keeps its defaults bit-identical. What does **not** move: provider credentials and
endpoints stay in `.env`, because a secret in a `config.json` is a secret in the repository.

New templates are added by the same rule that governs everything else in this repository: a
subtree plus its gates, no substrate change. Contributions are welcome; see
[`../CONTRIBUTING.md`](../CONTRIBUTING.md).
