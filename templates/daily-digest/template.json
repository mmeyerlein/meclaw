{
  "name": "daily-digest",
  "version": "2.0.3",
  "description": {
    "purpose": "Scheduled fetch-and-forward: a timer fires daily, a web_fetch pulls a URL, a code cell formats the result, a Telegram proxy delivers it to a fixed chat.",
    "use_when": "Periodic push of one URL's content to one Telegram chat. Override the URL, the cron expression and the chat id at instantiation. A parent may also demand a run at the hive path on lane in_digest, bringing context.chat_id itself, and take the formatted digest back on lane digest.",
    "not_in_scope": "No multi-source aggregation, no LLM summarization (insert an llm cell between format and notifier for that), no per-user subscriptions.",
    "examples": [
      "08:00 daily -> GET $DIGEST_URL -> 'Daily digest:\\n<first 3500 chars>' to $TELEGRAM_DIGEST_CHAT_ID"
    ]
  },
  "tags": [
    "timer",
    "digest",
    "telegram",
    "scheduled"
  ],
  "author": "@mmeyerlein",
  "license": "MIT"
}
