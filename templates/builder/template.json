{
  "name": "builder",
  "version": "1.1.0",
  "description": {
    "purpose": "The intake that turns a structural wish into a MANIFEST -- an ordered list of mutation declarations, ready to be submitted by whoever asked for it. Two classes on one lane: a FAST LANE of predefined parameterised recipes that runs without calling a model at all, and a DESIGN LANE that consults the builder's own corpus and asks one. It never applies anything: no cell in this template has an edge onto the control plane, which is a property of the files rather than a promise in this text.",
    "use_when": "Once per colony, at the OS level, beside the capability broker and the control loop -- a level owns what its siblings must share (ADR-0013), and one baumeister, one corpus and one audit trail is exactly what every organisation under it shares. Reached on `in_build` at the hive path; the draft comes back on `manifest`, a refusal on `error`.",
    "not_in_scope": "No application, no mutation, no rescan, no write to disk of any kind -- the requester submits, under its own identity, through the submitter that stands beside this hive. No approval decision either: whether a draft may be applied is decided where it is submitted, not where it is drawn. And no second corpus: retrieval is `builder-librarian`, referenced, not copied.",
    "examples": [
      "hop.build_op == 'draft' with a known `recipe` -> the fast lane renders the manifest deterministically, no model, no network",
      "a plain-language request with no recipe -> librarian brief -> one model call -> a normalised manifest plus its sha256 digest",
      "a request the composer answers with prose and no JSON -> `error`, error_code `no_manifest_in_answer`, and the build stops rather than shipping an empty one"
    ]
  },
  "tags": ["builder", "manifest", "hive", "composition", "fast-lane"],
  "author": "@mmeyerlein",
  "license": "MIT"
}
