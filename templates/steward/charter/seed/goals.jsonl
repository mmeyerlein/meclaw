{"schema": {"id": "text", "metric": "text", "direction": "text", "window_minutes": "int", "min_samples": "int", "min_delta_pct": "int", "quality_gate": "text", "enabled": "int", "note": "text"}}
{"id": "goal:llm-cost", "metric": "llm_cost", "direction": "lower", "window_minutes": 1440, "min_samples": 30, "min_delta_pct": 10, "quality_gate": "answer_quality", "enabled": 0, "note": "Lower model cost at equal answer quality. The launch demo -- and the reason quality is a GATE below, not a second goal: a cheaper colony that answers worse has not improved, it has been degraded."}
{"id": "goal:dlq-watch", "metric": "dlq_rate", "direction": "observe", "window_minutes": 60, "min_samples": 1, "min_delta_pct": 0, "quality_gate": "", "enabled": 0, "note": "Observe-only. It reports and never proposes: an error rate is a symptom, and a control loop that reacts to symptoms without a hypothesis is a random walk with receipts."}
