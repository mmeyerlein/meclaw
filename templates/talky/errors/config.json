{
  "cell": {
    "type": "code"
  },
  "params": {
    "runner": "python3",
    "script_inline": "\nimport sys, json\ndoc = json.load(sys.stdin)\nd = doc[\"body\"]\nenvelope = doc[\"envelope\"]\nh = envelope.get(\"header\") or {}\nhop = h.get(\"hop\") or {}\nctx = h.get(\"context\") or {}\nroute = str(hop.get(\"route\") or \"\")\nfinish = str(hop.get(\"finish_reason\") or \"\")\nif route == \"summary_error\":\n    source = \"summarizer\"\nelif route == \"reject\":\n    # The session-keeper's store refusal (GH #343). It is not a failed\n    # inference, so it carries no finish_reason -- and its code sits in\n    # hop.store_error, because the cell that reports it did not itself fail:\n    # the store it spoke to did.\n    source = \"session-keeper\"\nelif finish:\n    source = \"brain\"\nelse:\n    source = \"unknown\"\ncode = str(hop.get(\"error_code\") or hop.get(\"store_error\") or finish or \"error\")\nsession = str(hop.get(\"session_id\") or ctx.get(\"session_id\") or \"\")\nturn = str(hop.get(\"turn_id\") or ctx.get(\"turn_id\") or \"\")\ntext = \"talky error: source=%s code=%s session=%s turn=%s\" % (source, code, session, turn)\nsys.stdout.write(json.dumps([{\n    \"header\": {\"route\": \"error\", \"error_code\": code, \"error_source\": source,\n               \"session_id\": session, \"turn_id\": turn},\n    \"messages\": [{\"origin\": \"assistant\", \"type\": \"text\", \"text\": text}]}]))\n",
    "external_timeout_ms": 10000
  },
  "contract": {
    "version": "1.0.0",
    "settings": {},
    "multi_send_capable": true,
    "emits": {
      "body": {
        "messages": {
          "type": "array",
          "required": true
        }
      },
      "hop": {
        "route": {
          "type": "string",
          "values": [
            "error"
          ],
          "required": true
        },
        "error_code": {
          "type": "string",
          "required": false
        },
        "error_source": {
          "type": "string",
          "required": false
        },
        "session_id": {
          "type": "string",
          "required": false
        },
        "turn_id": {
          "type": "string",
          "required": false
        }
      }
    },
    "consumes": {
      "body": {
        "messages": {
          "type": "array",
          "required": true
        }
      }
    },
    "capabilities": [
      "shell:exec"
    ]
  },
  "description": {
    "purpose": "The composite's ONE error drain port: four failure lanes from three cells -- the brain's error and its content filter, the summarizer's failed summary, the session-keeper's store refusal -- fan in here and leave as one normalised report on route error, so a parent tree drains a single edge instead of four lanes.",
    "use_when": "Inside the talky composite. The parent MUST wire the exit somewhere - a log sink, an alarm, an operator surface; an unwired error lane dead-letters, which is loud but not actionable.",
    "not_in_scope": "No retry, no judgement, no swallowing: what a failure means is the parent's decision. Deliberately NOT a drain cell - a drain that ends inside the unit would make the failure invisible to the tree that owns the agent.",
    "emits_meaning": "route error: one report per failure, naming source (brain, summarizer or session-keeper), error code, session and turn. For a keeper refusal the code is the STORE's error_code, read off hop.store_error -- the keeper itself did not fail, the store it spoke to did (GH #343). The report carries NO conversation content on purpose - the llm error lane echoes the PROMPT back, and a drain is not the place to file it.",
    "consumes_meaning": "the brain's error / content_filter emission (finish_reason on the hop) the summarizer's summary_error emission (route on the hop), and the session-keeper's reject emission (route on the hop, hop.store_error / hop.store_operation beside it). Anything else is reported as source unknown rather than swallowed.",
    "examples": [
      "provider 500 at the brain -> one report, source brain, code error",
      "empty summary at the close -> one report, source summarizer, code empty_summary",
      "the session store refuses the nightly sweep -> one report, source session-keeper, code query_timeout"
    ]
  }
}
