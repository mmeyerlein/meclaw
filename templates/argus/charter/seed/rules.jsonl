{"schema": {"id": "text", "kind": "text", "value": "text", "note": "text"}}
{"id": "rule:radius", "kind": "radius", "value": "model,numeric_params", "note": "Mutation radius v1. The argus may change which model a cell runs and numeric params (caps, tiers). Topology it may only PROPOSE. The radius widens by editing this row -- never by editing code. What it can REACH is narrower than what it may decide: a params update lands only on a cell type that has a params lane, so an `llm` cell's model and numeric knobs are in range and a `code` cell's cap (collector `max_iter`) is not -- see `params.numeric_param_keys` of `./mutator`."}
{"id": "rule:revert-plan", "kind": "require_revert_plan", "value": "1", "note": "A cycle without a pre-authored revert plan is invalid. The argus must know the way back BEFORE it moves, or the first surprise leaves the colony in a state nobody designed."}
{"id": "rule:budget", "kind": "budget_eur_per_cycle", "value": "1", "note": "Hard cap per cycle. A control loop that can spend without a ceiling is an outage waiting for a slow night."}
{"id": "rule:quality-floor", "kind": "quality_floor_pct", "value": "0", "note": "How far the quality metric may move against us before a change is reverted, in percent. Zero means: not at all. Quality is a gate, not a wish."}
{"id": "rule:probe", "kind": "probe_timeout_ms", "value": "30000", "note": "How long the post-change health check may take before the cycle treats the colony as unhealthy and reverts."}
{"id": "rule:prices", "kind": "price_per_mtok", "value": "", "note": "Prices per million tokens as `model=input/output,model=input/output`. Charter DATA rather than a provider call: a colony that has to reach the network to know what it spent cannot measure itself while the network is what broke. Empty on a fresh instance -- an unpriced model measures as zero cost, which is why the cost goal ships disabled."}
