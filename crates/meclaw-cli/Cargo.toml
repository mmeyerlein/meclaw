[package]
name = "meclaw-cli"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "CLI/daemon library for MeClaw — install the `meclaw` crate for the binary; the library API is not stable."
repository = "https://github.com/mmeyerlein/meclaw"
license.workspace = true

[[bin]]
name = "meclaw"
path = "src/main.rs"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["json", "env-filter", "fmt"] }
tracing-appender = "0.2"
console-subscriber = "0.4"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "signal", "time"] }
serde_json = "1"
uuid = { version = "1", features = ["v7"] }
meclaw-api = { path = "../meclaw-api", version = "0.17.2" }
meclaw-cells = { path = "../meclaw-cells", version = "0.17.2" }
meclaw-colony = { path = "../meclaw-colony", version = "0.17.2" }
meclaw-core = { path = "../meclaw-core", version = "0.17.2" }

[dev-dependencies]
tempfile = "3"
rusqlite = { version = "0.39", features = ["bundled"] }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
meclaw-testing = { path = "../meclaw-testing" }
