# The unwrap/expect ceiling per workspace package (GH #115).
# Format: <package> <max clippy::unwrap_used + clippy::expect_used hits>
# Measured by .github/gates/unwrap_budget.py over the default targets
# (lib + bins) -- test modules and tests/ are out of scope by design.
# A number may only ever go DOWN. Re-pin with `unwrap_budget.py --write`
# and say in the commit why the number moved.
#
# meclaw-cells 71 -> 72 (wave voice-cell, 2026-09-05): one `expect` on the
# `cell.db` open inside a long-running factory's build closure. The closure is
# the `RespawnFn`, whose signature returns the spawn tuple and has nowhere to
# put a failure, and `build_long_running_task` needs a `DbConn`, which needs a
# `rusqlite::Connection` -- there is no value to fall back to. `web`, `timer`,
# `mcp` and `proxy` each carry the same call site for the same reason; a new
# cell type of that kind costs exactly one. The other two the `voice` cell would
# have taken were paid off instead of pinned: `split_io` is typed
# `Option<VoiceIo>` rather than unwrapped, and the session lookup in `handle`
# uses a let-else.
#
# meclaw-cells 72 -> 74 (wave G, GH #766): the `browser` cell type's build
# closure, which is the same call site the voice note above describes -- the
# `cell.db` open, and beside it the idempotent DDL of the `pages` table. Both
# sit inside a `RespawnFn` that returns the spawn tuple and has nowhere to put a
# failure. `harness` carries the same pair for the same reason; a new
# long-running type with a table of its own costs exactly two. The three this
# wave would otherwise have taken were paid off instead of pinned: the page
# lookup in the browser's `join` and the kind lookup in the web socket's join
# arm are let-elses that refuse, and `split_io` is typed `Option<BrowserIo>`
# rather than unwrapped -- both of those run in a loop where a panic takes a
# person's screen with it.
#
# meclaw-cells 74 -> 75 (wave peer, GH #815): the `meclaw` platform of `proxy`
# has its own build closure, and it carries the one `expect` on the `cell.db`
# open the voice note above describes, for the same reason. It writes nothing to
# that database (no DDL, no overlay), so it costs one, not two. `split_io` falls
# back to an unmounted I/O half instead of unwrapping, and the peer client is
# built outside the closure, so its failure is a spawn error.
#
# meclaw-testing 46 -> 45 (#653, 2026-09-11): back to what the tree measures.
# The 46 was pinned for an `expect` on the bind in `surface_listener` (#654); the
# fixture that shipped retries `free_port` instead and ends in an explicit
# `panic!` with the port and the error, which this ceiling does not count. A pin
# above the measurement measures nothing.

meclaw 0
meclaw-api 2
meclaw-cells 75
meclaw-cli 0
meclaw-colony 64
meclaw-core 3
meclaw-surface 0
meclaw-testing 45
