{
  "cell": {
    "type": "code"
  },
  "params": {
    "runner": "python3",
    "script_inline": "\nimport sys, json\nd = json.load(sys.stdin)\nquestion = d.get(\"header\", {}).get(\"context\", {}).get(\"question\", \"\")\nthread = []\nif question:\n    thread.append({\"origin\": \"user\", \"type\": \"text\", \"text\": question})\nthread.extend(d.get(\"messages\", []))\nsys.stdout.write(json.dumps({\"messages\": thread}))\n",
    "external_timeout_ms": 10000
  },
  "contract": {
    "version": "1.0.0",
    "settings": {},
    "consumes": {
      "body": {
        "messages": {
          "type": "array",
          "required": true
        }
      }
    }
  }
}
