{
  "cell": {
    "type": "code"
  },
  "params": {
    "runner": "python3",
    "script_inline": "\nimport sys\n# Terminal sink. The final answer already lives in the message trace\n# (open /ui/trace). Swallow so nothing dead-letters.\nsys.stdout.write(\"[]\")\n",
    "external_timeout_ms": 10000
  },
  "contract": {
    "version": "1.0.0",
    "settings": {},
    "consumes": {
      "body": {
        "messages": {
          "type": "array",
          "required": true
        }
      }
    },
    "multi_send_capable": true,
    "emits": {
      "body": {
        "messages": {
          "type": "array",
          "required": true
        }
      }
    }
  }
}
