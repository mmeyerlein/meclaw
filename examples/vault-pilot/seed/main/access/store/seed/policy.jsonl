{"schema": {"capability": "text", "constraints": "json", "cred_ref": "text", "enabled": "int", "max_ttl_ms": "int", "note": "text", "priority": "int", "requester": "text", "rule_id": "text", "scope_match": "json", "subject": "text", "verdict": "text"}}
{"capability": "credential.read", "constraints": {"rate_per_min": 6}, "cred_ref": "cred:example-provider:primary", "enabled": 1, "max_ttl_ms": 31536000000, "note": "The pilot's one live rule, and the only seeded row in this tree that ships enabled 1. It is NOT what lets the delivery below happen -- ./invoke reads `grants`, never `policy` -- it is what lets this grant be MINTED AGAIN when the seeded one expires: an access.request for credential.read on behalf of member:example answers `granted` instead of `capability_unknown`. scope_match names the one action a credential grant is good for, so a grant cut from this rule can spend nothing else.", "priority": 100, "requester": "agent:brain", "rule_id": "pilot-credential-read", "scope_match": {"actions": ["vault.deliver"]}, "subject": "member:example", "verdict": "allow"}
