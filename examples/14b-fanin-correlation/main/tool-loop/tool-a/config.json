{
  "cell": {
    "type": "code"
  },
  "params": {
    "runner": "python3",
    "script_inline": "\nimport sys, json\nd = json.load(sys.stdin)\nturns = d.get(\"messages\", [])\ncall = next((t for t in turns if t.get(\"type\") == \"tool_call\"), None)\ncid = call.get(\"id\",\"\") if call else \"\"\n# DISTINCT result text — proves a->tool-a routing (load-bearing discriminator)\ntext = \"A-ok\"\nout = {\"header\":{\"route\":\"c_res\"},\n       \"messages\":[{\"origin\":\"tool\",\"type\":\"tool_result\",\"id\":cid,\"text\":text}]}\nsys.stdout.write(json.dumps(out))\n",
    "external_timeout_ms": 10000
  },
  "contract": {
    "version": "0.1.0",
    "settings": {},
    "multi_send_capable": false,
    "emits": {
      "body": {
        "messages": {
          "type": "array"
        }
      },
      "hop": {
        "route": {
          "type": "string",
          "values": ["c_res"]
        }
      }
    },
    "consumes": {
      "body": {
        "messages": {
          "type": "array",
          "required": true
        }
      }
    }
  }
}
