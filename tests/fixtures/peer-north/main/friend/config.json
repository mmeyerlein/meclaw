{
  "cell": {
    "type": "proxy"
  },
  "params": {
    "platform": "meclaw",
    "mount": "peer",
    "identity_header": "X-Meclaw-Peer",
    "boundary": "north",
    "emit_to": "/",
    "external_timeout_ms": 5000,
    "lanes": {
      "accepts": [],
      "emits": [
        {
          "route": "topic",
          "fields": [
            "topic"
          ],
          "because": "a subject this side offers, never who said it"
        },
        {
          "route": "gossip",
          "fields": [
            "topic"
          ],
          "because": "a lane this side declares and no side accepts, so a refusal has something to refuse"
        }
      ]
    }
  },
  "contract": {
    "version": "1.0.0",
    "settings": {},
    "emits": {
      "body": {
        "messages": {
          "type": "array",
          "required": false
        }
      },
      "hop": {
        "route": {
          "type": "string",
          "required": true
        },
        "peer": {
          "type": "string",
          "required": false
        },
        "boundary": {
          "type": "string",
          "required": false
        },
        "peer_event": {
          "type": "string",
          "required": false
        },
        "lane": {
          "type": "string",
          "required": false
        },
        "fields": {
          "type": "array",
          "required": false
        },
        "error_code": {
          "type": "string",
          "required": false
        }
      }
    },
    "consumes": {
      "body": {
        "messages": {
          "type": "array",
          "required": true
        }
      }
    },
    "ingress": {
      "carries_trace": true
    }
  }
}
